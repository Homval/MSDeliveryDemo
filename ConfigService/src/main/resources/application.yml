server:
  port: 8888

spring:
  application:
    name: config-service
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
      server:
        git:
          uri: https://github.com/Homval/MSDeliveryDemo/ms-configs-properties/{application}/{profile}
#          searchPaths: "/ms-configs-properties/{application}/{profile}"
#          default-label: main
#          username:
#          password:


eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8762/eureka}
